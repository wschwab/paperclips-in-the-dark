---
title: Independent gate evidence — Luna reviews and final integration results
description: Committed record of the independent Luna review gates (Waves 3, 6, 7, final) and the Wave 8 integration gate results
---

# Independent gate evidence

Dated record of the independent review gates required by the ordered work
specification and `AGENTS.md` (GPT 5.6 Luna via `openai-codex`, reasoning
effort `xhigh`, read-only sandbox) plus the Wave 8 integration results.
Each gate ended in exactly `VERDICT: PASS` (anything else fails the gate and
is fixed before the next round).

> **Authoritative current evidence (2026-08-30).** The counts below for the
> final VET-01 gate reflect the 2026-08-30 candidate (`65d09ff8`), verified by
> `agent-docs/test-audit/final-vetting.md` (VET-01 Final Gate Evidence) and
> `tasks/metrics/contract/VET-01.json`. The Wave-8 table further below is
> preserved **as historical context** — it records the 2026-08-14 Wave-8
> state and is **not** an authoritative current claim.

## Wave 3 — contract/oracle correctness review (2026-08-14)

- **Rounds:** 15 (14 × `VERDICT: FAIL`, then `VERDICT: PASS`).
- **Scope:** Waves 1–3 (amended PAPERCLIPS, frozen contract + schemas +
  settings + generators, managed harness, frozen red oracle, SPARK clock
  Posts).
- **Findings fixed across rounds:** read-purity vs migrate-on-GET (explicit
  repair only), turf/formatVersion schema alignment, summary-row `kind`,
  INSUFFICIENT_FUNDS funds detail shape, preview document/token schema,
  numeric-family declarations, harness startup/signal robustness (spawn-time
  PID registration, vitest-child tracking, port retry), strict-decode
  boundary, and more.
- **Evidence:** the FAIL→PASS round transcripts and the fix commits are in the
  wave history; final verdict recorded in the orchestrator session log.

## Wave 6 — multimodal UI gate (2026-08-14)

- **Rounds:** 5 (4 × `VERDICT: FAIL`, then `VERDICT: PASS`).
- **Scope:** frontend clients (SC-F1…F6, G1), real-Chromium evidence
  (52 screenshots + numeric containment/focus/a11y), light + dark.
- **Findings fixed:** strict `onExcessProperty:"error"` at the client API
  boundary, repairable deep-link import rendered raw 422, raw transport text
  in create/health alerts, missing 404 route (FV-030 closed early), torn-seam
  heading clip (FV-031 closed early).
- **Evidence:** `agent-docs/frontend-vetting/wave6-evidence-r2/`
  (gitignored local evidence; numeric entries + screenshots).

## Wave 7 — craft-evidence multimodal review (R3) (2026-08-14)

- **Rounds:** 1 × `VERDICT: PASS`.
- **Scope:** the eight craft findings (P08/P12/P13/P14/P15/P16/P25/P32;
  P30/P31 closed in Wave 6) — 71 PNGs + numeric geometry + source.
- **Evidence:** `agent-docs/frontend-vetting/wave7/` (gitignored local
  evidence).

## Final VET-01 whole-initiative gate (2026-08-30)

Candidate `65d09ff8`; final Luna review (REVIEW-02) on 2026-08-30 returned
`VERDICT: PASS`. The authoritative 2026-08-30 results, recorded in
`agent-docs/test-audit/final-vetting.md` (VET-01 Final Gate Evidence) and
`tasks/metrics/contract/VET-01.json`:

| Gate | Command | Result |
|---|---|---|
| Ada build + core tests + launch paths + SPA routes + whole-core proof | `./backend-ada/ci.sh` | green; gnatprove 246/246 checks proved |
| Full conformance against a fresh seeded server | `RUN_CONFORMANCE=1 ./backend-ada/ci.sh` | **471/471** |
| Conformance (test:ada) | `cd conformance && npm run test:ada` | **471/471** (58 files) |
| Frontend suite | `cd frontend && npx vitest run` | **759/759** (20 files) |
| Frontend build | `(cd frontend && npm run build)` | green; 676 modules |
| Conformance tooling | `cd conformance && npm run test:tooling` | **170/170** (13 files) |
| Mutation (full catalog) | `cd conformance && npm run test:mutation` | **28/28 killed** (P0 19/19, P1 9/9) |
| Browser journeys | `cd conformance && npm run test:browser` | **6/6 journeys** (0 problems) |
| Benchmark | `cd conformance && npm run test:benchmark` | green (scales 100 + 1000) |
| OpenAPI lint | `npx -y @redocly/cli lint contract/openapi.yaml --config contract/redocly.yaml` | 0 errors, 1 warning |
| Operation disposition coverage | coverage map | **110/110 operations, 345/345 dispositions** |

**VET-01 extras (8 spec items, 7 PASS + 1 PENDING):**

1. **Two corruption/repair cycles** (`test:ada --cycles 2`): Cycle 1 → 471/471; Cycle 2 → 471/471 (Total 942/942). Per-cycle repair-related test count: 21 (of 26 registrations in the three canonical-shape/persistence suites; exact enumeration and inclusion rule below).
2. **Generator idempotency twice** (`TOOLING-GEN-001..004`): completeness module, API reference, capability manifest, Ada validator metadata — all byte-identical on second run (SHA256 verified).
3. **Default-data guard negative calibration** (spec line 1292): default-data inventory 1525 rows, 0 deletions, `data/games` 10 files unchanged.
4. **Six browser journeys + full viewport/theme matrix**: 6/6 PASS, 9 matrix entries per journey, no horizontal overflow.
5. **Generated-doc agent workflow exercise — PENDING**: The black-box workflow at `/tmp/gen-doc-workflow-final.mjs` completed all 13 steps using only generated docs, but it spawned the server on **fixed port 9666** and exited **without awaiting server termination or directory removal**, leaving six `gen-doc-workflow-*` directories under `/tmp/pitd-managed/`. This violates the "zero leaked resources" requirement. **Evidence marked PENDING** awaiting the isolation worker's cleaned managed rerun. Generator idempotency (sub-criterion b) is independently satisfied.
6. **Track Z expected-red**: `docs/pages/zero/z4-halted-expected-red.mdx` present; non-blocking. Static verification only.
7. **Ledger/coverage/deletion artifact validation**: 1525 inventory rows confirmed; deletion-log zero rows; contract coverage 345/345 (deterministic, ORACLE-01).
8. **Campaign-data manifest comparison + preservation**: Root `campaign-data/` 4003 files, 17,495,126 bytes, combined hash `eb1a292524061bf759c6dd92ac9146ab4004d1b4ea56ca61aeb3b976b6380d88` — identical before/after. Durable manifest refreshed at `agent-docs/test-audit/campaign-data-manifest.json` (was stale: 1457 files, 6,134,087 bytes from 2026-08-23).
9. **Manifest**: operationCount 110 = 86 agent + 24 human + 0 exempt; approvedExemptions: []; flaggedCandidates count = **8** (`batch`, `createCharacter`, `createPcCharacter`, `stressAdd`, `harmAdd`, `harmRemove`, `createCrew`, `createClock`).

**Repair-related per-cycle count (exact 21):** the three canonical-shape/persistence suites (`canonical-shape.test.ts` with 13, `import-repair.test.ts` with 7, `concurrency-tokens.test.ts` with 6 = 26 total) register 26 cases per cycle. Of these, **21** exercise the import/repair/preview/apply/normalization-token contract. The **inclusion rule**: a test is repair-related if it exercises any of: (a) import preview/apply (409 NORMALIZATION_REQUIRED with preview token), (b) repair preview/apply on a degraded stored entity, (c) deprecated-format migration (legacy notes string → array, v1 character/crew → canonical), (d) If-Match / preview-token / revision / deleteToken validation on the repair or delete path, (e) 409 STALE_REVISION for stale-token on degraded entity, (f) read-purity guards that pin the "never silently repair on read" contract.

The exact 21:

- **canonical-shape.test.ts (8):** CANON-IMPORT-003, CANON-IMPORT-004, CANON-IMPORT-005, CANON-IMPORT-006, CANON-LEGACY-007, CANON-REPAIR-008, CANON-READONLY-009, CANON-TOTAL-013
- **import-repair.test.ts (7):** REPAIR-TOKEN-001, REPAIR-TOKEN-002, REPAIR-TOKEN-003, REPAIR-ATOMIC-004, FV-MIGRATE-CHAR-001, FV-MIGRATE-CREW-001, FV-NOTES-STRING-001
- **concurrency-tokens.test.ts (6):** CONC-IFMATCH-001, CONC-IFMATCH-002, CONC-IFMATCH-003, CONC-REV-004, CONC-TOKEN-005, CONC-NOWRITE-006

The remaining 5 are general canonical-shape guards (CANON-CREATE-001, CANON-NULL-002, CANON-NOWRITE-010, CANON-VERSION-011, CANON-SPARSE-012) that do not touch the repair path. The prior enumeration incorrectly included `ERR-DETAIL-004` (from `error-union.test.ts`, NOT one of the three persistence suites) and omitted CONC-REV-004, CONC-TOKEN-005, and CONC-NOWRITE-006.

**Strict decoders:** ordinary current-version decoders contain no compatibility defaults or legacy error branches (EDGE-02 / AR-012). Verified by Luna REVIEW-02 (6/6 journeys) and VET-01 mutation (28/28 killed).

**Luna PASS:** GPT 5.6 Luna via `openai-codex` at `xhigh` independently ran
`npm run test:browser` (fresh run `2026-08-30T06-14-37-794Z-d819482b`:
`passed=true`, 0 problems, 6/6 journeys) and inspected source.
`VERDICT: PASS`.

**Tooling correction:** VET-01.json previously stated 156/156 tooling; the
corrected log `04-test-tooling.log` records 170/170 (13 test files). The
working copy's `conformance/package.json` has `test:ada` removed (present in
parent commit `wvzskznt`); this causes `TOOLING-MANAGED-025` to fail in the
current working copy but is a pre-existing state, not introduced by these
corrections.

**Campaign-data manifest refresh:** `agent-docs/test-audit/campaign-data-manifest.json`
regenerated 2026-08-30 using the repository's `buildCampaignManifest` function
(`conformance/scripts/default-data-guard.mjs`); records 4003 files,
17495126 bytes, identical schema to the 2026-08-23 snapshot (which had
1457 files, 6134087 bytes). No campaign bytes were modified.

**AR-007 finding-traceability update:** independent review of all 195 P0/P1
rows + decision classes completed; VERDICT: PASS; references
`agent-docs/test-audit/p0p1-independent-review.md`.

Notes:

- The conformance suite is deterministic: `fileParallelism: false` in
  `conformance/vitest.config.ts` (a black-box suite must run files
  sequentially against one server) and `backend-ada/ci.sh` seeds the standard
  oracle fixtures before starting its server.
- `nonBlankString` is Unicode-whitespace on both the Ada server and the
  generated frontend evaluator (ECMAScript TrimString set); the completeness
  oracle pins an NBSP case (COMPLETE-WHITESPACE-004).
- **extra05-manifest.log correction:** the original log at
  `/tmp/vet01-raw-exact-final/extra05-manifest.log` exited 1 because the
  inventory validation looked for `conformance/generated/inventory.json`
  (does not exist). Re-ran from repo root using the correct path
  `agent-docs/test-audit/inventory.json` → exit 0. See
  `/tmp/vet01-raw-exact-final/extra05-manifest-fixed.log`.

## Historical Wave 8 integration gate (2026-08-14) — preserved as context only

The following table records the 2026-08-14 Wave-8 snapshot. It is **historical**
and **not** an authoritative current claim; the VET-01 table above supersedes it.

| Gate | Command | Historical result (2026-08-14) |
|---|---|---|
| Ada build + core tests + launch paths + SPA routes + whole-core proof | `./backend-ada/ci.sh` | green; gnatprove 246/246 checks proved |
| Full conformance against a fresh seeded server | `env RUN_CONFORMANCE=1 ./backend-ada/ci.sh` | **415/415** |
| Frontend suite | `(cd frontend && npm test -- --run)` | **598/598** |
| Frontend build | `(cd frontend && npm run build)` | green |
| Conformance tooling | `(cd conformance && npm run test:tooling)` | **21/21** |
| Generator idempotency | run generators twice, `sha256sum` diff | byte-identical |
| Golden fixtures + settings | ajv 2020-12 + ajv-draft-04 | all valid |
| Corruption/repair suites | managed-run, twice, fresh dirs | **22/22** twice |
| Agent workflows | exercised from generated docs only | matched docs |
| Track Z expected-red | `docs/pages/zero/z4-halted-expected-red.mdx` | recorded; non-blocking |

## Final whole-initiative review

- **AR-007 finding-traceability update:** independent review of all 195 P0/P1 rows
  + decision classes completed (VERDICT: PASS) via `agent-docs/test-audit/p0p1-independent-review.md`.
  The `finding-traceability.md` AR-007 row text has been updated to reflect
  `independentReviewStatus: reviewed` (not `pending`) with review evidence path.
- **Tooling count:** corrected from stale 156/156 to 170/170.
- **Campaign-data manifest:** refreshed from stale 1457/6134087 to current 4003/17495126.
- **extra05-manifest.log:** inventory validation corrected to use
  `agent-docs/test-audit/inventory.json` path (exit 0).
- **Generated-doc agent workflow:** PENDING (fixed-port run with leaked temp dirs;
  awaiting isolation worker's cleaned managed rerun).

Full verdict transcript: `docs/pages/contract/gate-evidence/final-verdict.md`.
