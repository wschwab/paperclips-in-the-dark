---
title: 32-finding traceability ledger — FV-001..FV-032 ownership, red evidence, and closure
description: Authoritative per-finding ledger linking every confirmed frontend-vetting finding to exactly one owning card in the ordered work spec, correcting the superseded draft's blanket closure claims
---

# 32-finding traceability ledger

**Status:** authoritative for Wave 7 closure; produced by SC-R6 (Wave 0).
**Date:** 2026-08-14.
**Governing documents:** [Ordered work specification](/contract/spec-change-work-spec) (waves and SC-O8 constraints are normative); `agent-docs/frontend-vetting/BUGS.json` and `agent-docs/frontend-vetting/bugs/FV-001.json`…`FV-032.json` (finding register and per-finding records); `agent-docs/FRONTEND-FIX-WAVE-PLAN.md` (fix-wave P-cards); the superseded [DRAFT — spec and contract change plan](/contract/spec-change-draft) Part 7 (rationale only; its closure claims are corrected below).

## Purpose and rules

This ledger is the authoritative traceability source for Wave 7 individual finding closure and informs SC-O8 (FV-specific oracle corrections) and the Wave 8 integration gate. Every finding FV-001..FV-032 has **exactly one owning card** — the implementation owner whose behavioral proof closes the finding. Shared infrastructure (canonicalization, completeness, typed errors) may appear as a dependency but never substitutes for the finding's own focused reproduction.

Owner naming: non-craft findings are owned by work-spec cards (`SC-A*` Waves 4–5, `SC-F*`/`SC-G1` Wave 6, `SC-C6` contract-only); the ten craft findings keep their fix-wave P-cards (`P08`, `P12`–`P16`, `P25`, `P30`–`P32`, Wave 7). Evidence paths are relative to the vetting run root `/tmp/pitd-vetting-moBdOm` as recorded in the per-finding JSON.

## Severity register

Source: `agent-docs/frontend-vetting/BUGS.json` `bySeverity` (32 findings, all `status: confirmed`):

| Severity | Count | Findings |
|---|---|---|
| P0 | 2 | FV-001, FV-002 |
| P1 | 8 | FV-003..FV-010 |
| P2 | 12 | FV-011..FV-022 |
| P3 | 10 | FV-023..FV-032 |

## The ledger — 32 rows

| FV | Severity | Original reproduction (evidence path) | Owning wave/card | Dependencies | Red evidence | Expected green evidence | Status |
|---|---|---|---|---|---|---|---|
| FV-001 | P0 | Legacy formatVersion-1 characters (rev 58, ids `9ab91681…`, `69426100…`) lack `dossier.vice.purveyor`; `decodeCharacterEither` → Left; error card "This character sheet could not be loaded" via direct URL and roster click (`evidence/s02/get-9ab9.json`, `evidence/s02/legacy-9ab9-error.webp`, `evidence/s02/decode-matrix.json`) | Wave 4 · **SC-A2** | SC-C1 (canonical vice schema), SC-A1 (canonicalizer), SC-F1 (decoder accepts canonical shape); fix-wave: O-MIGRATION, O-PURVEYOR, P03 | Original s02 GET/decode captures; frozen oracle FV-MIGRATE-CHAR-001 (import-repair.test.ts) fails red: direct GET of the legacy v1 character returns `200` with no repair path, instead of `422 INVALID_ENTITY` (repairable, preview available) | FV-MIGRATE-CHAR-001 green: direct GET of the legacy v1 character → `422 INVALID_ENTITY` (repairable, preview available); repair-preview → `409 NORMALIZATION_REQUIRED` listing the `dossier.vice.purveyor` fill; confirmed repair-apply → `200` canonical; stored bytes untouched before apply (checksum) and second read byte-identical — no migration write ever happens on GET (fix-wave P01) | Open — confirmed |
| FV-002 | P0 | Legacy crew `855994b4-3821-49a7-81b7-f184789af507` lacks `turf`, `claimedClaimIds`, `claimOverrides`; `decodeCrewEither` fails at first missing required key (turf) (`evidence/s02/get-crew-8559.json`, `evidence/s18/`) | Wave 4 · **SC-A2** | SC-C1 (crew canonical schema), SC-F1; fix-wave: O-MIGRATION, P03 | Original s02 payload/key diff; frozen oracle FV-MIGRATE-CREW-001 (import-repair.test.ts) fails red: direct GET of the legacy v1 crew returns `200` with no repair path, instead of `422 INVALID_ENTITY` (repairable, preview available) | FV-MIGRATE-CREW-001 green: direct GET of the legacy v1 crew → `422 INVALID_ENTITY` (repairable); repair-preview → `409 NORMALIZATION_REQUIRED` listing the `turf:0` (L7) and `claimedClaimIds:[]`/`claimOverrides:[]` fills; confirmed repair-apply → `200` canonical; stored bytes untouched before apply and second read byte-identical — no migration write ever happens on GET; claims remain unclaimed and Turf invariant unchanged (fix-wave P02) | Open — confirmed |
| FV-003 | P1 | Mutation write-through: `note.add` on planted defects (c06 commitment `''`, c11 revision 0, c12 stress.max −1) returns ok:true with the defect intact in response and on disk; revision 0 → 2 from an invalid base (`evidence/s17/`, `post-c06.json`, `post-c11.json`, `post-c12.json`) | Wave 4 · **SC-A2** | SC-A1 (canonicalizer covers every write path), SC-C1; fix-wave: O-MIGRATION | Original post/get defect pairs; frozen idempotency / future-version / metadata migration cases red | Migration suite green; second read changes zero bytes; unknown future `formatVersion` returns typed failure and never writes; crash hook leaves only a valid old or approved-new shape (fix-wave P03) | Open — confirmed |
| FV-004 | P1 | UI cohort add always sends `hasArmor`; server rejects "unknown field" → 400 VALIDATION, no cohort created, while the contract allows `hasArmor`/`edges`/`flaws`/`description` (`evidence/s13-r2/raw/cohort-add-hasarmor-400.json`) | Wave 5 · **SC-A9** | SC-C6 (cohort.add retained contract correction); oracle O-COHORT | Original 400 capture; O-COHORT red: allowed-field request returns 400 unknown field, unknown-field control also 400 | `-t "cohort.add"` conformance green; allowed payload creates gang and expert; unknown control stays 400 (fix-wave P04) | Open — confirmed |
| FV-005 | P1 | `upgrade.mark` has no cap: Secure Lair (TotalBoxes=2) marked 3→10, all HTTP 200 ok:true; `UPGRADE_MAXED` never emitted; UI renders "10 / 2" (`evidence/s12/upgrade-mark-at3.json`, `evidence/s12/upgrade-overmarked-10of2.webp`) | Wave 5 · **SC-A6** | SC-C3 (settings-derived TotalBoxes), SC-C6 (retained correction); oracle O-UPGRADE | Original mark series; O-UPGRADE red: cap+1 succeeds instead of `UPGRADE_MAXED` | `-t "upgrade.mark"` conformance green; cap−1, cap, cap+1, unknown upgrade, unmark correct for two settings fixtures (fix-wave P05) | Open — confirmed |
| FV-006 | P1 | `Clocks.Progress` executes `Overflow := Amount − Applied` (replace): 6th "+1" on a 4/4 rollover clock keeps rollover 1 instead of 2; carried segments silently lost (`evidence/S08/clock-rollover-progress.json`, `evidence/s08/`) | Wave 5 · **SC-A7** | SC-C6 (rollover accumulation retained), SC-O5 (SPARK `Post` before implementation); oracle O-CLOCK | Original S08 probe sequence; O-CLOCK red: two progress calls past full fail to accumulate prior overflow | Core test `rollover progress accumulates existing overflow` green + changed-unit gnatprove + whole clock conformance file green (fix-wave P06) | Open — confirmed |
| FV-007 | P1 | Healing-clock "+1 segment" sends **absolute** `segments` while backend applies **delta**: clicks from 0/4 land at 1, 3, 4+3, 4+5; after a heal the clock stays 4/4 (rollover 1) and heals again without earning segments (`evidence/S05/monitor-matrix.json`) | Wave 6 · **SC-F3** | SC-A7 (healing rollover semantics), SC-C2 (declared operation family), SC-O5 | Original monitor matrix; focused page test red: character at 5/6 sends absolute `{segments:6}` instead of delta `{segments:1}` | `character-detail.test.ts` green; browser executes two +1 actions around rollover and persisted/displayed values agree with no phantom consecutive heal; screenshot (fix-wave P07) | Open — confirmed |
| FV-008 | P1 | Long unbroken text (300-char name/alias/notes/history) explodes layout: document scrollWidth 4253px vs 420px client; no overflow-wrap/word-break anywhere (`evidence/S27Stress/shots/long-char-sheet-420.webp`, `evidence/s27/`) | Wave 7 · **P08** (craft) | P18, P32 | Geometry red: stress fixture at 420×900 measures document width ≥4095 px | Roster/detail/history page files green; Chromium 320/360/390/420/1024: document width equals client width, ordinary prose unchanged; `geometry.json` + light/dark/forced-colors screenshots; Luna R3 PASS | Open — confirmed |
| FV-009 | P1 | Legacy string `.notes` renders (frontend union) but `note.add` → server `Constraint_Error` → generic "invalid operation arguments"; notes journey blocked on legacy records (`raw/k04-noteadd.json`, `raw/k04-get1.json`) | Wave 4 · **SC-A2** | SC-A1 (known legacy conversion: one notes string → one-entry notes array), SC-C1; fix-wave: O-MIGRATION, P03 interface | Original k04 note-add capture; frozen oracle FV-NOTES-STRING-001 (import-repair.test.ts) fails red: the string-notes legacy record is directly readable (`200`) and `note.add` still fails, instead of `422 INVALID_ENTITY` with preview/apply repair | FV-NOTES-STRING-001 green: direct GET of the string-notes legacy record → `422 INVALID_ENTITY` (repairable); repair-preview → `409 NORMALIZATION_REQUIRED` listing the L1 conversion (legacy string → one-entry `notes` array); confirmed repair-apply → `200` canonical; then `note.add`/`note.remove` twice without loss; stored bytes untouched before apply and second read byte-identical — no migration write ever happens on GET or mutation (fix-wave P09) | Open — confirmed |
| FV-010 | P1 | One character missing `.monitor` takes down the ENTIRE roster: `GET /api/campaign/roster` → 400 discriminant check; `GET /api/characters` → 413; roster error card; removing the record restores 200 (`evidence/s17/roster-error.json`, `evidence/s17/list-characters-no-c09.json`) | Wave 4 · **SC-A4** | SC-A3 (typed classification, 422 direct GET), SC-C1 (total summary rows), SC-C4; oracle O-ROSTER | Original 400/413 pair; O-ROSTER red: one malformed member breaks every valid row | `-t "roster isolates malformed entity"` conformance green; valid rows preserved at 200, degraded row keeps route identity with `isReadable:false` and stays deletable; server healthy (fix-wave P10) | Open — confirmed |
| FV-011 | P2 | XP "−" buttons silently no-op: `attribute-xp.add`/`playbook-xp.add`/crew `xp.add` with delta −1 → 200 `effective:0`; DOM and disk unchanged (rep.add/heat.add accept −1) (`evidence/S26/ops-negative-deltas.json`, `evidence/S26/ops-min-capture.json`) | Wave 5 · **SC-A6** | SC-C6 (signed XP deltas retained), SC-C2 (declared family); oracle O-XP | Original S26 captures; O-XP red: delta −1 applies 0 on all currently no-op negative paths | XP semantics conformance file green for crew, attribute, playbook: 1→0, 0→0, positive delta, clear; whole detail test files green (fix-wave P11) | Open — confirmed |
| FV-012 | P2 | Focus lost to BODY after every mutation: wholesale re-render replaces the activated control (`evidence/s22/`, `evidence/s26/`) | Wave 7 · **P12** (craft) | P04, P07, P11, P14, P15, P19, P21, P24, P28, P29 (mutation cards whose controls P12 restores) | Page tests red: add/update/delete controls leave focus on `document.body` after success | Both full detail test files green; focus returns to replacement trigger, new-item control, or next sibling after deletion for success, failure, and stale refresh; active-element trace + a11y snapshot at 420×900/1024×900; Luna R2 then R3 | Open — confirmed |
| FV-013 | P2 | Focus stays on BODY after every client-side route navigation; zero `.focus()` calls in `frontend/src`; skip-link/main infrastructure unused (`evidence/s01/s01-character-sable.webp`, `evidence/s22/`) | Wave 7 · **P13** (craft) | P30 | `main.test.ts` red: route navigation leaves BODY focused | `main.test.ts` + `shell.test.ts` green; click navigation and browser Back focus page heading/main; skip-link still works; focus trace + a11y snapshot; Luna R2 then R3 | Open — confirmed |
| FV-014 | P2 | Faction-entry status input lacks `min-width:0`: 7.89 px bleed at 420 (403.89 vs card 396.00); document scroll 404 at 320/360 (`evidence/S14/narrow-420-faction-entry.webp`, `evidence/S14/narrow-420-section-closeup.png`) | Wave 7 · **P14** (craft) | none | Geometry red: at 420×900 the row bleeds 7.89 px; at 320/360 document width exceeds viewport | `crew-detail.test.ts` green; every child rect within the card at 320/360/390/420; contact row unchanged; row rects + screenshots; Luna R3 | Open — confirmed |
| FV-015 | P2 | Harm add row overflows below ~390px: nowrap flex select+input+button without `min-width:0`; document scrollWidth 391 at 360 (`evidence/s23/`) | Wave 7 · **P15** (craft) | P07 | Geometry red: 320/360/390 routes measure document width 391 px or an input outside the health card | `character-detail.test.ts` green; controls remain operable at 320/360/390/420; no global overflow suppression; control rects + screenshots; Luna R3 | Open — confirmed |
| FV-016 | P2 | App-bar nav + theme controls bleed 4 px at 320 px on every route (document scrollWidth 324) (`evidence/s23/`) | Wave 7 · **P16** (craft) | P13, P30 | Geometry red: every 320 route reports document width 324 and app-bar right > viewport | `shell.test.ts` green; app-bar rect equals viewport at 320/360/420 in all themes; nav/theme controls remain visible and focusable; rect matrix + screenshots; Luna R3 | Open — confirmed |
| FV-017 | P2 | Create phase-2 naming failure (dossier.update 500) leaves an unnamed orphan plus a dead-end "Failed to create" error; manual retry creates a SECOND entity (`evidence/S20/create-phase2-error.webp`, `evidence/S20/final-roster.json`) | Wave 6 · **SC-F6** | none | Extended no-duplicate tests red: no usable link/retry after phase-two failure; retry POSTs create again | Create test files green; phase-one success + phase-two failure + two retries keeps entity count one and reaches the sheet; fetch call sequence + final entity list (fix-wave P17) | Open — confirmed |
| FV-018 | P2 | `renderCrew` renders an empty `<strong>` for `name:''` — unnamed crews are blank, indistinguishable roster rows, unlike the character fallback (`evidence/S10/unnamed-roster.webp`) | Wave 6 · **SC-F6** | none | `roster.test.ts` red: blank link for `{name:"", crewType:"Assassins"}` | `roster.test.ts` green; link text is `Unnamed Assassins`, href/id unchanged, named crews unchanged; DOM extract + light/dark screenshots (fix-wave P18) | Open — confirmed |
| FV-019 | P2 | Undo is not staleness-guarded: `undoCharacter`/`undoCrew` send no `If-Match`; stale-tab undo silently reverts a concurrent stress.add (rev 24 → 25, stress 2 → 1) (`evidence/s09/character-final.json`, `evidence/s09/history-final.json`) | Wave 6 · **SC-F5** | SC-C2 (required concurrency headers) | `client.test.ts` red: undo requests lack `If-Match`; detail tests red: 409 does not refresh | Client and both detail test files green; undo header equals current revision; 409 refreshes without undoing concurrent state; success unchanged (fix-wave P19) | Open — confirmed |
| FV-020 | P2 | Create pages have no error UI for game-data load failure: plain text `Failed to load playbooks: ApiError: API 500: boom`, no h1/retry/back, developer class name leaked (`evidence/s19/recovery-matrix.json` scenario 7, `evidence/s19/`) | Wave 6 · **SC-F5** | none | `main.test.ts` red: plain developer text / no h1 / no retry on failed `getPlaybookList`/`getCrewTypeList` | `main.test.ts` + `shell.test.ts` green; recoverable error card has one h1, retry, roster escape, friendly category, no `ApiError:` leakage; DOM/a11y snapshot + light/dark screenshots; Luna R2 then R3 (fix-wave P20) | Open — confirmed |
| FV-021 | P2 | Action dots advertise max 4 while the effective Mastery-gated cap is 3 for crewless characters: 4th dot always fails RATING_MAXED; no client-side clamp/disable (`evidence/s06-talent-playbook/rating-maxed.json`, `evidence/s06-talent-playbook/action-dots-rated3.png`) | Wave 6 · **SC-F3** | SC-A6 (unified effective cap), SC-C3 (capability projections), SC-C6; oracle O-RATING | Decoder/page tests red: crewless DTO advertises dot 4 and lets the user send a request that always returns `RATING_MAXED` | Decoder + detail test files green; UI renders O-RATING's effective cap, cap button absent/disabled, Mastery DTO still exposes the higher settings-derived cap; dot screenshots; Luna R2 then R3 (fix-wave P21) | Open — confirmed |
| FV-022 | P2 | `attribute.levelup` caps at maxRating 4 while `action.set-rating` caps at Mastery cap 3 for the same crewless character; UI can show 4/4 the setter refuses (`evidence/s06-talent-playbook/insight-xp-levelup.webp`, `evidence/s06-talent-playbook/rating-maxed.json`) | Wave 5 · **SC-A6** | SC-C3, SC-C6 (shared effective cap retained); oracle O-RATING | O-RATING red: levelup reaches 4 while direct set rejects 4 for a non-Mastery character | Action/attribute semantics conformance file green for crewless, non-Mastery, and Mastery fixtures; paired operation responses and DTOs (fix-wave P22) | Open — confirmed |
| FV-023 | P3 | Malformed 200 JSON classified as network failure: status-0 `ApiError` "Failed to reach … (0): Unexpected token 'o'…" — parser text leaked into the trace (`evidence/s19/recovery-matrix.json` scenario 5) | Wave 6 · **SC-F5** | SC-C4 (discriminated error union) | `client.test.ts` red: malformed 200 JSON becomes status-0 `ApiError` and leaks parser text | `client.test.ts` green; malformed JSON → `DecodeError`, rejected HTTP → `ApiError`, thrown fetch → network `ApiError`; pages show distinct friendly copy (fix-wave P23) | Open — confirmed |
| FV-024 | P3 | HTTP-level operation errors dump raw OperationResult JSON into a plain `<p class='error'>` without `role="alert"`: `API error (500): {"ok":false,...}` (`evidence/S20/mutation-error.webp`) | Wave 6 · **SC-F5** | SC-C4 | Page tests red: HTTP OperationResult renders as raw JSON without an alert role | Both detail test files green; known codes map to user copy, unknown code has concise fallback, no raw DTO string, alert role announced; a11y snapshot; Luna R2 then R3 (fix-wave P24) | Open — confirmed |
| FV-025 | P3 | Create-page form controls have no accessible names: name input placeholder-only; playbook/crew-type selects unlabeled (`evidence/S21/create-form-controls.json`) | Wave 7 · **P25** (craft) | P17 | Create tests red: empty accessible names for all three controls | Create test files green + Chromium a11y snapshots; unique IDs and label associations survive validation/retry; light/dark screenshots; Luna R2 then R3 | Open — confirmed |
| FV-026 | P3 | Contract mismatch: `dossier.update` request schema types vice as `namedDescription` (no purveyor) while the server accepts/stores purveyor; contract documents the shape wrong (`evidence/s02/post-update.json`, `evidence/s02/get-mutated.json`) | Wave 2 · **SC-C6** (contract-only) | fix-wave oracle O-PURVEYOR (no implementation P-card) | `c4-playtest.test.ts` red: a full vice object including `purveyor` is contract-valid; omission of required `purveyor` is contract-valid (wrong polarity) | `suites/semantics/c4-playtest.test.ts` green; full vice object incl. purveyor contract-valid and persists byte-for-byte; omission of required purveyor contract-invalid (fix-wave O-PURVEYOR) | Open — confirmed |
| FV-027 | P3 | Three-way divergence: contract `additionalProperties:false` rejects top-level `.xYzExtra` (ajv invalid); frontend decoder ignores it (Right); backend preserves it through reads and mutations (`raw/k12-get1.json`, `raw/frontend-decode.json`, `raw/contract-check.json`) | Wave 4 · **SC-A3** (dual-card — see note) | SC-C1/SC-C4 (contract side), SC-A2 (migration before admission), SC-F1 (second required implementation card; fix-wave P27B, P27A counterpart) | O-EXTRA counterexample matrix red: structural counterexamples returned or propagated as normal DTOs; decoder accepts unknown top-level/nested keys | `suites/persistence/strict-entity-validation.test.ts` + decoder matrix green; every bounded admission counterexample returns frozen 422 with no write for each enumerated operation; controls and optional crew fields pass; invalid imports stay 400; residual-risk list unchanged (fix-wave P27A + P27B) | Open — confirmed |
| FV-028 | P3 | Successful undo renders no positive feedback (`undoNotice = null`); create leaves no baseline snapshot so the first undo of a fresh entity always says NO_HISTORY (`evidence/s09/undo-no-history.webp`, `evidence/s16/`) | Wave 6 · **SC-F4** | SC-A8 (create baseline snapshot / undo helpers), SC-C6 (`canUndo`/`historyCount`) | Detail tests red: successful undo clears every notice and gives no feedback | Both detail test files green; success notice names restored state, `NO_HISTORY` remains distinct, live semantics appropriate; a11y snapshot; Luna R2 then R3 (fix-wave P28) | Open — confirmed |
| FV-029 | P3 | Crew tracker clamps are silent: turf/heat/coin hit server-side bounds with no `applied.requested/effective` notice; bound buttons stay enabled and no-op (`evidence/s11-r2/`) | Wave 6 · **SC-F3** | SC-A6 (requested/effective semantics), SC-C4 | `client.test.ts`/`crew-detail.test.ts` red: bound +/- remains enabled or no-ops silently; clamped effective delta produces no notice | Client + crew-detail files green; bound controls disabled, requested/effective notice displayed, persisted value equals UI, ordinary applied delta stays quiet (fix-wave P29) | Open — confirmed |
| FV-030 | P3 | Unknown paths serve the SPA shell with HTTP 200 and render the legacy health page ("API health"); create loading state transiently has no h1 (`evidence/s01/s01-unknown.webp`) | Wave 7 · **P30** (craft) | none | `main.test.ts` red: unknown path mounts health; create preload has zero h1 | `main.test.ts`/`shell.test.ts` green; unknown route shows one 404 h1 + roster link and does not fetch health; both create preloads always have one h1; DOM/fetch trace + screenshots; Luna R2 then R3 | Open — confirmed |
| FV-031 | P3 | App-bar torn seam (14 px, z-index 2) overlaps any page whose first heading sits flush below the bar: seam bottom > h1 top on create, history, and error states (`evidence/wave0/affected-char-error-seam-overlap.webp`, `evidence/s01-route-reach/s01-character-create.webp`) | Wave 7 · **P31** (craft) | P20, P30, P32 | Geometry red: measured `h1.top < appBar::after.bottom` on create, history, and error states | Create/history/shell files green + Chromium 420/1024 in light/dark/forced-colors; every first h1 clears the seam and no new gap appears under sheet mastheads; seam/h1 geometry + screenshots; Luna R3 | Open — confirmed |
| FV-032 | P3 | History pages have no Back-to-sheet link in their normal (non-error) state; empty-history state is a bare "(no history snapshots)" with no explanation or affordance (`evidence/S27Stress/shots/empty-history.webp`, `evidence/S27Stress/shots/populated-history-longname.webp`) | Wave 7 · **P32** (craft) | none | History tests red: normal/empty state has no sheet link and only "(no history snapshots)" | Both history test files green; normal and empty states include correct back link and explanatory copy; error behavior unchanged; DOM extracts + 420×900 light/dark screenshots; Luna R2 then R3 | Open — confirmed |

Status legend: `Open — confirmed` = registered in BUGS.json with a verified reproduction, not yet closed (Wave 0 has no implementation). Wave 7 flips a row to `Closed` only with the finding's own reproduction passing plus the owning card's focused evidence.

## Corrections — replaced blanket closures from the draft Part 7

Every row of the superseded draft's Part 7 table (`docs/pages/contract/spec-change-draft.mdx`) is replaced below. The draft closed findings by broad mechanism ("E4/B6"-style); the corrected owner is the single card whose focused behavioral proof closes the finding. Note the draft's Part 8 "no format-version bump, and no migration engine (D1, D2)" is reversed by the approved repair contract (persistence v2 with atomic, confirmed repair — O-MIGRATION's migrate-on-GET plan is superseded by the work spec: reads are pure and never migrate), which is why the migration findings can no longer be "legacy records become drafts".

| # | Draft Part 7 claim (superseded) | Finding(s) | Corrected owner(s) | Why |
|---|---|---|---|---|
| 1 | E1, E3, B1, B2 — legacy records become drafts | FV-001, FV-002, FV-003, FV-009 | FV-001 → SC-A2; FV-002 → SC-A2; FV-003 → SC-A2 (deps SC-A1); FV-009 → SC-A2 | Draft Part 8 explicitly had no migration engine; the approved spec chooses persistence v2 with **explicit, previewed repair** (O-MIGRATION's migrate-on-GET plan is superseded by the work spec): direct GET of a legacy v1 record → `422 INVALID_ENTITY` (repairable, preview available), repair-preview → `409 NORMALIZATION_REQUIRED`, confirmed repair-apply → `200` canonical, and reads never write. Legacy v1 records are **repaired on confirmation** (SC-A2 known legacy conversion, atomic apply), not reclassified as drafts. Closure still requires each finding's own reproduction (decode, direct GET 422, preview/apply repair, mutation on the repaired data) |
| 2 | E4, B6 — roster isolation plus operable incomplete entities | FV-010 | SC-A4 (deps SC-A3, SC-C1, SC-C4) | B6's per-member isolation is correct and now lives in SC-A4 (total collections and degraded deletion); the "operable incomplete entities" framing is superseded by the total-row shape (unreadable rows stay listed with `isReadable:false` and a `deleteToken`) plus typed 422 on direct GET |
| 3 | E4, E9, F2 — drafts are resumable and labelled | FV-017, FV-018 | FV-017 → SC-F6; FV-018 → SC-F6 | Per SC-O8, neither finding is completeness-driven. Both are create-flow UX: retain the created entity and link/retry without duplicating (FV-017) and deterministic `Unnamed {playbook\|crewType}` labels (FV-018) — exactly SC-F6 |
| 4 | C9 | FV-026 | SC-C6 (contract-only; fix-wave O-PURVEYOR) | Draft C9 (vice → `$defs.vice`) is carried forward verbatim into SC-C6's retained contract corrections. Contract-only: no implementation P-card exists; SC-A9's "vice request parity" verifies the server boundary but does not own closure |
| 5 | E4, C3, C5, B2, F1 — narrowed to undeclared keys and unreadable bytes | FV-027 | SC-A3 + SC-F1 (dual-card; fix-wave P27A + P27B) | The narrowing is retained (canonical shape removed missing-key errors), but closure needs two implementation owners: backend bounded storage admission with typed 422 (SC-A3) and frontend strict recursive decoders rejecting undeclared keys (SC-F1). The ledger row names SC-A3 as single owner with SC-F1 as required dependency |
| 6 | B2 (`Crew_Required`) and the already-correct cohort contract | FV-004 | SC-A9 (deps SC-C6) | Per SC-O8, FV-004 is **not** an entity-admission finding. The defect is the server's `cohort.add` field check rejecting contract-allowed fields; owner is SC-A9 (retained server defects — "cohort allowed fields" is named there), with the contract correction in SC-C6 |
| 7 | E5, C6, B3 | FV-005, FV-021, FV-022 | FV-005 → SC-A6; FV-021 → SC-F3; FV-022 → SC-A6 | E5/C6/B3 bundled three different defects. FV-005 and FV-022 are backend enforcement (SC-A6: upgrade maxima; unified action/level-up cap). FV-021 is the frontend consumer of the effective cap (SC-F3: capability projections; deps SC-A6, SC-C3, SC-C6) |
| 8 | E6, C8, B3, B4, F4 | FV-006, FV-007, FV-011, FV-029 | FV-006 → SC-A7; FV-007 → SC-F3; FV-011 → SC-A6; FV-029 → SC-F3 | B4's rollover fix is correct but is now SC-A7. Per SC-O8, FV-007 is **not** backend rollover alone — the phantom heal is the frontend sending absolute segments instead of the declared delta family (SC-F3, deps SC-A7/SC-C2). FV-011 is backend signed-delta acceptance (SC-A6). FV-029 is frontend clamp communication (SC-F3) |
| 9 | E7, C4, B5, F4 | FV-020, FV-023, FV-024 | FV-020 → SC-F5; FV-023 → SC-F5; FV-024 → SC-F5 | Per SC-O8, FV-020/FV-023/FV-024 are **not** typed-domain-errors alone. All three are transport/error-presentation defects owned by SC-F5 (distinguish malformed JSON from network/HTTP/domain failures, recovery actions, accessible alerts, no parser/raw-JSON leakage); SC-C4 is the dependency that feeds them. FV-028 is NOT part of this group — see row 11 (SC-F4) |
| 10 | E8, C5, F3 | FV-019 | SC-F5 (deps SC-C2) | E8/C5 declare the concurrency rule; the finding's behavioral proof is the frontend undo path sending `If-Match` and routing 409 through the stale-refresh path — SC-F5 "preserve stale-revision refresh" |
| 11 | E9, F4 | FV-028 | SC-F4 (deps SC-A8, SC-C6) | Not a typed-error defect. The closure is lifecycle UX: positive undo feedback plus a create baseline snapshot so first undo is not NO_HISTORY — SC-F4 (undo/history state), with SC-A8 providing the baseline/undo helpers |
| 12 | unchanged fix-wave craft cards (F5) | FV-008, FV-012–016, FV-025, FV-030–032 | FV-008 → P08; FV-012 → P12; FV-013 → P13; FV-014 → P14; FV-015 → P15; FV-016 → P16; FV-025 → P25; FV-030 → P30; FV-031 → P31; FV-032 → P32 | Preserved, now individually owned. The F5 bundle reference is replaced by the ten named craft cards (see next section); no craft finding is closed by sharing a theme with canonicalization, completeness, or typed errors |

Also noted: the draft's closing sentence ("Also closed, though never logged as findings: absent character/crew delete UI and absent retirement UI (E9, F3, capability parity)") refers to unlogged capabilities, not FVs. They remain covered as capabilities: degraded-row repair/delete controls (SC-F2), retirement UI (SC-F4), and parity manifest dispositions (SC-C5, SC-G1, SC-O7) — outside the 32-row ledger.

## Ten craft findings preserved with P-card owners

The ten craft findings remain independent frontend cards (work spec Wave 7; fix-wave plan Phase 2/3) and may run concurrently after their dependencies:

| Craft finding | Owning P-card | Card (fix-wave plan) | Deps |
|---|---|---|---|
| FV-008 — unbroken-text containment | P08 | Phase 3 responsive/visual | P18, P32 |
| FV-012 — mutation focus restoration | P12 | Phase 2 behavior/a11y | P04, P07, P11, P14, P15, P19, P21, P24, P28, P29 |
| FV-013 — route focus | P13 | Phase 2 behavior/a11y | P30 |
| FV-014 — faction-row containment | P14 | Phase 3 responsive/visual | none |
| FV-015 — harm-add-row containment | P15 | Phase 3 responsive/visual | P07 |
| FV-016 — app-bar 320 px bleed | P16 | Phase 3 responsive/visual | P13, P30 |
| FV-025 — create accessible names | P25 | Phase 2 behavior/a11y | P17 |
| FV-030 — real 404 route and loading h1 | P30 | Phase 2 behavior/a11y | none |
| FV-031 — torn-seam heading clearance | P31 | Phase 3 responsive/visual | P20, P30, P32 |
| FV-032 — history navigation and empty state | P32 | Phase 2 behavior/a11y | none |

## Contract-only and dual-card findings

- **Contract-only (no implementation P-card):** **FV-026** — owned by Wave 2 · SC-C6 (retained contract corrections; fix-wave oracle card O-PURVEYOR). The server already accepts/stores `purveyor`; only the contract documents the wrong shape. SC-A9's "vice request parity" verifies the server boundary but does not own the finding.
- **Dual-card (two required implementation owners):** **FV-027** — needs **SC-A3** (backend bounded storage admission and typed 422; fix-wave P27A) **and SC-F1** (frontend strict generated decoders; fix-wave P27B). The ledger row's single owner cell names SC-A3; SC-F1 is recorded as a required dependency and both must land for the finding's behavior (backend rejects, frontend rejects, contract stays `additionalProperties:false`) to close.
- **Two-sided (frontend-primary, backend dependency):** **FV-007** (SC-F3 primary, SC-A7 healing-clock semantics) and **FV-028** (SC-F4 primary, SC-A8 create baseline snapshot) — the secondary card is a dependency, not an alternative owner.
- No finding is owned by SC-G1, SC-C5, SC-A1, SC-A5, or SC-A8 as primary; those cards provide shared infrastructure that appears only in Dependencies columns.

## Evidence

- **Severity counts:** P0 = 2, P1 = 8, P2 = 12, P3 = 10; source `agent-docs/frontend-vetting/BUGS.json` `bySeverity`. All 32 records `status: confirmed`.
- **Superseded draft Part 7 excerpt being corrected:** see the 12-row table above; the original text (draft lines 354–373) is:

  > | Findings | Closed by |
  > |---|---|
  > | FV-001, FV-002, FV-003, FV-009 | E1, E3, B1, B2 — legacy records become drafts |
  > | FV-010 | E4, B6 — roster isolation plus operable incomplete entities |
  > | FV-017, FV-018 | E4, E9, F2 — drafts are resumable and labelled |
  > | FV-026 | C9 |
  > | FV-027 | E4, C3, C5, B2, F1 — narrowed to undeclared keys and unreadable bytes |
  > | FV-004 | B2 (`Crew_Required`) and the already-correct cohort contract |
  > | FV-005, FV-021, FV-022 | E5, C6, B3 |
  > | FV-006, FV-007, FV-011, FV-029 | E6, C8, B3, B4, F4 |
  > | FV-020, FV-023, FV-024 | E7, C4, B5, F4 |
  > | FV-019 | E8, C5, F3 |
  > | FV-028 | E9, F4 |
  > | FV-008, FV-012–016, FV-025, FV-030–032 | unchanged fix-wave craft cards (F5) |

- **Example reproduction → owner mapping method (FV-006):** the record (`agent-docs/frontend-vetting/bugs/FV-006.json`) reproduces the lost carry-over with a UI sequence plus an API probe (`evidence/S08/clock-rollover-progress.json`, `evidence/s08/`), and names the faulty code path (`backend-ada/core/src/paperclips_core-clocks.adb:14-16`, `Overflow := Amount - Applied`). Work spec SC-A7 ("Implement the proven rollover contract…", with SC-O5 stating the SPARK `Post` first) is the single owner; SC-C6 carries the retained rollover-accumulation contract correction; the fix-wave plan's P06 provides the concrete red (core test `rollover progress accumulates existing overflow`) and green (changed-unit gnatprove, whole `core_tests`, complete clock conformance file) evidence. Same method applied to all 32 rows: reproduction → defective surface/path → owning SC-A*/SC-F*/P* card → frozen oracle red case → owning card's focused acceptance.

## Wave 3 oracle pins (SC-O8, frozen 2026-08-14)

The frozen oracle pins each finding's behavior. `pinned-by` = the oracle case — or, for frontend findings, the owning card's page test — whose red/green proves the finding; API-level cases named alongside are support only. `owner` stays the implementation card from the ledger above.

| FV | Pinned by (frozen oracle case) | Owner |
|---|---|---|
| FV-001 | FV-MIGRATE-CHAR-001 (import-repair.test.ts) | SC-A2 |
| FV-002 | FV-MIGRATE-CREW-001 (import-repair.test.ts) | SC-A2 |
| FV-003 | ADMIT-REVISION-010 + ADMIT-MUTATION-012 (entity-admission.test.ts) | SC-A2 |
| FV-004 | ERR-FV004-001 (error-union.test.ts) | SC-A9 |
| FV-005 | LIMIT-UPGRADE-005 (published-limits.test.ts) | SC-A6 |
| FV-006 | CLOCK-ROLLOVER-006 (clocks.test.ts) + SPARK Post (paperclips_core-clocks.ads) | SC-A7 |
| FV-007 | SC-F3 page test (P07); API support: NUM-CLOCK-004 (numeric-families.test.ts) + CLOCK-RESULT-014 | SC-F3 |
| FV-008 | craft card P08 (Wave 7) | P08 |
| FV-009 | FV-NOTES-STRING-001 (import-repair.test.ts) | SC-A2 |
| FV-010 | TOTAL-200-001/TOTAL-ROW-002/TOTAL-CREW-005 (total-collections.test.ts) | SC-A4 |
| FV-011 | NUM-SIGNED-001 (numeric-families.test.ts) | SC-A6 |
| FV-012 | craft card P12 (Wave 7) | P12 |
| FV-013 | craft card P13 (Wave 7) | P13 |
| FV-014 | craft card P14 (Wave 7) | P14 |
| FV-015 | craft card P15 (Wave 7) | P15 |
| FV-016 | craft card P16 (Wave 7) | P16 |
| FV-017 | SC-F6 page tests (no conformance substitute; API support: create keeps the entity) | SC-F6 |
| FV-018 | SC-F6 page tests (roster label; API support: COMPLETE-ALL-003 empty-name rows) | SC-F6 |
| FV-019 | CONC-IFMATCH-001/CONC-IFMATCH-002 (concurrency-tokens.test.ts) | SC-F5 |
| FV-020 | SC-F5 page tests (error-card); API support: ERR-UNION-001 union shapes | SC-F5 |
| FV-021 | LIMIT-CHAR-002 (published-limits.test.ts) | SC-F3 |
| FV-022 | LIMIT-RATING-004 (published-limits.test.ts) | SC-A6 |
| FV-023 | SC-F5 client tests (DecodeError); API support: ERR-UNION-001 | SC-F5 |
| FV-024 | SC-F5 page tests (alert role, no raw JSON); API support: ERR-NORAW-009 | SC-F5 |
| FV-025 | craft card P25 (Wave 7) | P25 |
| FV-026 | ERR-FV026-001 (error-union.test.ts, schema guard) | SC-C6 |
| FV-027 | ADMIT-KEY-003/ADMIT-KEY-004 (backend) + SC-F1 decoders.test.ts (frontend) | SC-A3 (deps: SC-F1 required) |
| FV-028 | LIFECYCLE-DERIVED-001 (lifecycle-state-machine.test.ts) + SC-F4 page tests | SC-F4 |
| FV-029 | NUM-QTY-003 (numeric-families.test.ts) + SC-F3 page tests | SC-F3 |
| FV-030 | craft card P30 (Wave 7) | P30 |
| FV-031 | craft card P31 (Wave 7) | P31 |
| FV-032 | craft card P32 (Wave 7) | P32 |

## Change log

- **2026-08-14:** Created by SC-R6 from BUGS.json, per-finding records, the ordered work spec (Waves 0–8, SC-O8 constraints), and the fix-wave plan. All 12 Part 7 blanket closures replaced with single-card owners; ten craft findings preserved with P-cards; FV-026 recorded contract-only; FV-027 recorded dual-card (SC-A3 + SC-F1).
- **2026-08-14 (Wave 3 freeze-gate corrections):** FV-007's pin designation corrected to the SC-F3 page test (P07), with NUM-CLOCK-004 and CLOCK-RESULT-014 as API-level support only (owner unchanged: SC-F3). FV-001/FV-002/FV-009 red and expected-green wording switched from migrate-on-GET to the explicit-repair contract (direct GET `422 INVALID_ENTITY`, repair-preview `409 NORMALIZATION_REQUIRED`, confirmed apply `200`, no migration write on GET or mutation); corrections table row 1 and its lead-in note updated to match.
