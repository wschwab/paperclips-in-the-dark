pragma SPARK_Mode (On);

package Paperclips_Core is
   --  A deliberately small proving target for A0. Domain types and contracts
   --  are added here by A1; the package remains IO-free by construction.
end Paperclips_Core;
