# Paperclips in the Dark API reference

> Generated from `contract/openapi.yaml` by `skill/generate-api-reference.mjs`. Do not edit this file by hand.

Base URL: `http://localhost:9657/api`

Operations: 87

## Conventions

- Mutations use JSON `POST` requests and return the uniform `OperationResult` described by the contract.
- `ifMatch` means the optional `If-Match` revision header; `idempotencyKey` means the optional `Idempotency-Key` header.
- A snapshot value reports the contract's `x-snapshot` setting for that mutation.

## Endpoint index

| Method | Path | Operation | Snapshot |
| --- | --- | --- | --- |
| GET | `/health` | [`health`](#health) | — |
| GET | `/campaign` | [`getCampaign`](#getcampaign) | — |
| GET | `/campaign/roster` | [`getRoster`](#getroster) | — |
| GET | `/campaign/crew/{crewId}/members` | [`getCrewMembers`](#getcrewmembers) | — |
| POST | `/campaign/batch` | [`batch`](#batch) | true |
| GET | `/games` | [`listGames`](#listgames) | — |
| GET | `/games/{stem}` | [`getGame`](#getgame) | — |
| GET | `/games/{stem}/playbooks` | [`listPlaybooks`](#listplaybooks) | — |
| GET | `/games/{stem}/playbooks/{playbook}` | [`getPlaybook`](#getplaybook) | — |
| GET | `/games/{stem}/heritages` | [`listHeritages`](#listheritages) | — |
| GET | `/games/{stem}/crews` | [`getCrewSettings`](#getcrewsettings) | — |
| GET | `/games/{stem}/crews/{crewType}` | [`getCrewTypeSettings`](#getcrewtypesettings) | — |
| GET | `/characters` | [`listCharacters`](#listcharacters) | — |
| POST | `/characters` | [`createCharacter`](#createcharacter) | false |
| GET | `/characters/{id}` | [`getCharacter`](#getcharacter) | — |
| POST | `/characters/{id}/delete` | [`deleteCharacter`](#deletecharacter) | false |
| POST | `/characters/{id}/import` | [`importCharacter`](#importcharacter) | true |
| GET | `/characters/{id}/history` | [`listCharacterHistory`](#listcharacterhistory) | — |
| GET | `/characters/{id}/history/{snapshotId}` | [`getCharacterSnapshot`](#getcharactersnapshot) | — |
| POST | `/characters/{id}/undo` | [`undoCharacter`](#undocharacter) | false |
| POST | `/characters/{id}/ops/stress.add` | [`stressAdd`](#stressadd) | true |
| POST | `/characters/{id}/ops/stress.clear` | [`stressClear`](#stressclear) | false |
| POST | `/characters/{id}/ops/trauma.add` | [`traumaAdd`](#traumaadd) | true |
| POST | `/characters/{id}/ops/trauma.remove` | [`traumaRemove`](#traumaremove) | true |
| POST | `/characters/{id}/ops/harm.add` | [`harmAdd`](#harmadd) | true |
| POST | `/characters/{id}/ops/harm.remove` | [`harmRemove`](#harmremove) | true |
| POST | `/characters/{id}/ops/harm.heal` | [`harmHeal`](#harmheal) | true |
| POST | `/characters/{id}/ops/harm.healing-clock` | [`harmHealingClock`](#harmhealingclock) | false |
| POST | `/characters/{id}/ops/armor.set` | [`armorSet`](#armorset) | false |
| POST | `/characters/{id}/ops/playbook-xp.add` | [`playbookXpAdd`](#playbookxpadd) | true |
| POST | `/characters/{id}/ops/playbook-xp.clear` | [`playbookXpClear`](#playbookxpclear) | true |
| POST | `/characters/{id}/ops/attribute-xp.add` | [`attributeXpAdd`](#attributexpadd) | true |
| POST | `/characters/{id}/ops/attribute-xp.clear` | [`attributeXpClear`](#attributexpclear) | true |
| POST | `/characters/{id}/ops/attribute.levelup` | [`attributeLevelup`](#attributelevelup) | true |
| POST | `/characters/{id}/ops/action.set-rating` | [`actionSetRating`](#actionsetrating) | true |
| POST | `/characters/{id}/ops/ability.take` | [`abilityTake`](#abilitytake) | true |
| POST | `/characters/{id}/ops/ability.remove` | [`abilityRemove`](#abilityremove) | true |
| POST | `/characters/{id}/ops/gear.add` | [`gearAdd`](#gearadd) | true |
| POST | `/characters/{id}/ops/gear.remove` | [`gearRemove`](#gearremove) | true |
| POST | `/characters/{id}/ops/gear.commit` | [`gearCommit`](#gearcommit) | false |
| POST | `/characters/{id}/ops/gear.uncommit` | [`gearUncommit`](#gearuncommit) | false |
| POST | `/characters/{id}/ops/gear.set-commitment` | [`gearSetCommitment`](#gearsetcommitment) | false |
| POST | `/characters/{id}/ops/gear.clear-commitments` | [`gearClearCommitments`](#gearclearcommitments) | true |
| POST | `/characters/{id}/ops/gear.lock` | [`gearLock`](#gearlock) | false |
| POST | `/characters/{id}/ops/gear.unlock` | [`gearUnlock`](#gearunlock) | false |
| POST | `/characters/{id}/ops/fund.gain` | [`fundGain`](#fundgain) | true |
| POST | `/characters/{id}/ops/fund.spend` | [`fundSpend`](#fundspend) | true |
| POST | `/characters/{id}/ops/fund.liquidate` | [`fundLiquidate`](#fundliquidate) | true |
| POST | `/characters/{id}/ops/rolodex.add` | [`rolodexAdd`](#rolodexadd) | true |
| POST | `/characters/{id}/ops/rolodex.remove` | [`rolodexRemove`](#rolodexremove) | true |
| POST | `/characters/{id}/ops/rolodex.set-closeness` | [`rolodexSetCloseness`](#rolodexsetcloseness) | true |
| POST | `/characters/{id}/ops/dossier.update` | [`dossierUpdate`](#dossierupdate) | true |
| POST | `/characters/{id}/ops/session.set` | [`sessionSet`](#sessionset) | false |
| POST | `/characters/{id}/ops/notebook.set` | [`notebookSet`](#notebookset) | false |
| POST | `/characters/{id}/end-score` | [`endScore`](#endscore) | true |
| POST | `/characters/{id}/end-downtime` | [`endDowntime`](#enddowntime) | true |
| GET | `/crews` | [`listCrews`](#listcrews) | — |
| POST | `/crews` | [`createCrew`](#createcrew) | false |
| GET | `/crews/{id}` | [`getCrew`](#getcrew) | — |
| POST | `/crews/{id}/delete` | [`deleteCrew`](#deletecrew) | false |
| POST | `/crews/{id}/import` | [`importCrew`](#importcrew) | true |
| GET | `/crews/{id}/history` | [`listCrewHistory`](#listcrewhistory) | — |
| GET | `/crews/{id}/history/{snapshotId}` | [`getCrewSnapshot`](#getcrewsnapshot) | — |
| POST | `/crews/{id}/undo` | [`undoCrew`](#undocrew) | false |
| POST | `/crews/{id}/ops/heat.add` | [`heatAdd`](#heatadd) | true |
| POST | `/crews/{id}/ops/wanted.add` | [`wantedAdd`](#wantedadd) | true |
| POST | `/crews/{id}/ops/rep.add` | [`repAdd`](#repadd) | true |
| POST | `/crews/{id}/ops/tier.add` | [`tierAdd`](#tieradd) | true |
| POST | `/crews/{id}/ops/hold.set` | [`holdSet`](#holdset) | true |
| POST | `/crews/{id}/ops/xp.add` | [`crewXpAdd`](#crewxpadd) | true |
| POST | `/crews/{id}/ops/xp.clear` | [`crewXpClear`](#crewxpclear) | true |
| POST | `/crews/{id}/ops/ability.take` | [`crewAbilityTake`](#crewabilitytake) | true |
| POST | `/crews/{id}/ops/ability.remove` | [`crewAbilityRemove`](#crewabilityremove) | true |
| POST | `/crews/{id}/ops/upgrade.mark` | [`upgradeMark`](#upgrademark) | true |
| POST | `/crews/{id}/ops/upgrade.unmark` | [`upgradeUnmark`](#upgradeunmark) | true |
| POST | `/crews/{id}/ops/cohort.add` | [`cohortAdd`](#cohortadd) | true |
| POST | `/crews/{id}/ops/cohort.update` | [`cohortUpdate`](#cohortupdate) | true |
| POST | `/crews/{id}/ops/cohort.remove` | [`cohortRemove`](#cohortremove) | true |
| POST | `/crews/{id}/ops/coin.add` | [`coinAdd`](#coinadd) | true |
| POST | `/crews/{id}/ops/stash.add` | [`stashAdd`](#stashadd) | true |
| POST | `/crews/{id}/ops/fields.update` | [`crewFieldsUpdate`](#crewfieldsupdate) | true |
| GET | `/clocks` | [`listClocks`](#listclocks) | — |
| POST | `/clocks` | [`createClock`](#createclock) | false |
| GET | `/clocks/{id}` | [`getClock`](#getclock) | — |
| POST | `/clocks/{id}/delete` | [`deleteClock`](#deleteclock) | false |
| POST | `/clocks/{id}/ops/clock.progress` | [`clockProgress`](#clockprogress) | false |
| POST | `/clocks/{id}/ops/clock.reset` | [`clockReset`](#clockreset) | false |

## health

`GET /health`

Health + agent-skill discovery

Parameters: none

Request body schema:

None

Responses:

- `200`: ok

## getCampaign

`GET /campaign`

campaign metadata

Parameters: none

Request body schema:

None

Responses:

- `200`: campaign metadata

## getRoster

`GET /campaign/roster`

all character + crew summaries

Parameters: none

Request body schema:

None

Responses:

- `200`: all character + crew summaries

## getCrewMembers

`GET /campaign/crew/{crewId}/members`

characters linked to this crew

Parameters: `crewId`

Request body schema:

None

Responses:

- `200`: characters linked to this crew
- `404`: OperationResult

## batch

`POST /campaign/batch`

Sequential all-or-nothing multi-op; single snapshot + history entry

Parameters: `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [ops]
  additionalProperties: false
  properties:
    ops:
      type: array
      minItems: 1
      maxItems: 50
      items:
        type: object
        required: [entity, id, op, args]
        additionalProperties: false
        properties:
          entity: { enum: [character, crew, clock] }
          id: { type: string }
          op: { type: string, description: "dotted op name, e.g. stress.add" }
          args: { type: object }
```

Responses:

- `200`: OperationResult

## listGames

`GET /games`

installed games

Parameters: none

Request body schema:

None

Responses:

- `200`: installed games

## getGame

`GET /games/{stem}`

full game-settings JSON (authored data, served verbatim from data/games/)

Parameters: `stem`

Request body schema:

None

Responses:

- `200`: full game-settings JSON (authored data, served verbatim from data/games/)
- `404`: OperationResult

## listPlaybooks

`GET /games/{stem}/playbooks`

playbook name + hook list

Parameters: `stem`

Request body schema:

None

Responses:

- `200`: playbook name + hook list
- `404`: OperationResult

## getPlaybook

`GET /games/{stem}/playbooks/{playbook}`

full playbook settings

Parameters: `stem`, `playbook`

Request body schema:

None

Responses:

- `200`: full playbook settings
- `404`: OperationResult

## listHeritages

`GET /games/{stem}/heritages`

heritage list

Parameters: `stem`

Request body schema:

None

Responses:

- `200`: heritage list
- `404`: OperationResult

## getCrewSettings

`GET /games/{stem}/crews`

crew game settings

Parameters: `stem`

Request body schema:

None

Responses:

- `200`: crew game settings
- `404`: OperationResult

## getCrewTypeSettings

`GET /games/{stem}/crews/{crewType}`

full crew type settings

Parameters: `stem`, `crewType`

Request body schema:

None

Responses:

- `200`: full crew type settings
- `404`: OperationResult

## listCharacters

`GET /characters`

character summaries

Parameters: none

Request body schema:

None

Responses:

- `200`: character summaries

## createCharacter

`POST /characters`

Parameters: `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [gameStem, playbook]
  additionalProperties: false
  properties:
    gameStem: { type: string, pattern: "^[A-Za-z0-9-]+$" }
    playbook: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult

## getCharacter

`GET /characters/{id}`

Full DTO; byte-identical to current.json on disk. ?download=1 = export.

Parameters: `id`, `download`

Request body schema:

None

Responses:

- `200`: character DTO
- `404`: OperationResult

## deleteCharacter

`POST /characters/{id}/delete`

Requires confirm:true (CONFIRM_REQUIRED otherwise). Removes entity and its history.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Confirm" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## importCharacter

`POST /characters/{id}/import`

Schema-validated full-DTO replacement; 1 MiB cap; clears history, takes one baseline snapshot.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Character" }
```

Responses:

- `200`: OperationResult
- `400`: OperationResult
- `404`: OperationResult
- `409`: OperationResult
- `413`: OperationResult

## listCharacterHistory

`GET /characters/{id}/history`

snapshot list, newest first

Parameters: `id`

Request body schema:

None

Responses:

- `200`: snapshot list, newest first
- `404`: OperationResult

## getCharacterSnapshot

`GET /characters/{id}/history/{snapshotId}`

snapshot DTO (read-only)

Parameters: `id`, `snapshotId`

Request body schema:

None

Responses:

- `200`: snapshot DTO (read-only)
- `404`: OperationResult

## undoCharacter

`POST /characters/{id}/undo`

Restore newest snapshot and delete it (consecutive undos walk backwards). No history → NO_HISTORY.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## stressAdd

`POST /characters/{id}/ops/stress.add`

Clamped delta (BoundedInteger semantics): applied.effective reports the clamp. At max stress a 'stress full — consider trauma' sideEffect is added; NEVER auto-trauma.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## stressClear

`POST /characters/{id}/ops/stress.clear`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## traumaAdd

`POST /characters/{id}/ops/trauma.add`

Separate explicit op (stress overflow never auto-traumas). Duplicate → DUPLICATE. Adding the max-th trauma retires the character (sideEffect).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [trauma]
  additionalProperties: false
  properties:
    trauma: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## traumaRemove

`POST /characters/{id}/ops/trauma.remove`

Allowed even when retired (spec §5.1.9 exception).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [trauma]
  additionalProperties: false
  properties:
    trauma: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## harmAdd

`POST /characters/{id}/ops/harm.add`

Spillover ladder: rolls UPWARD when slots full (lesser→moderate→severe→fatal). Landing intensity reported in applied.landedIntensity + sideEffect when it differs from requested. All full at/above requested → SLOT_FULL_FATAL.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [description, intensity]
  additionalProperties: false
  properties:
    description: { type: string, minLength: 1 }
    intensity: { $ref: "./schemas/common.json#/$defs/harmIntensity" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## harmRemove

`POST /characters/{id}/ops/harm.remove`

Exact intensity only; never cascades.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [description, intensity]
  additionalProperties: false
  properties:
    description: { type: string, minLength: 1 }
    intensity: { $ref: "./schemas/common.json#/$defs/harmIntensity" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## harmHeal

`POST /characters/{id}/ops/harm.heal`

Requires healing clock full (else CANNOT_HEAL). Resets clock (rollover applied), shifts moderate→lesser and severe→moderate; fatal never shifts or clears.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## harmHealingClock

`POST /characters/{id}/ops/harm.healing-clock`

Rollover clock; overflow past full is carried and applied on reset.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [segments]
  additionalProperties: false
  properties:
    segments: { type: integer }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## armorSet

`POST /characters/{id}/ops/armor.set`

Set used flag for one armor kind. Setting used=true without availability (loadout/ability-derived, exposed as monitor.armor.has*) → ARMOR_NOT_AVAILABLE. used=false always succeeds.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [armor, used]
  additionalProperties: false
  properties:
    armor: { enum: [standard, heavy, special] }
    used: { type: boolean }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## playbookXpAdd

`POST /characters/{id}/ops/playbook-xp.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## playbookXpClear

`POST /characters/{id}/ops/playbook-xp.clear`

Explicit clear (e.g. after spending a playbook advance). NOTE the asymmetry: ability.take does NOT clear playbook XP; only this op and attribute.levelup (attribute track only) clear XP.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## attributeXpAdd

`POST /characters/{id}/ops/attribute-xp.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [attribute, delta]
  additionalProperties: false
  properties:
    attribute: { type: string, minLength: 1 }
    delta: { type: integer }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## attributeXpClear

`POST /characters/{id}/ops/attribute-xp.clear`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [attribute]
  additionalProperties: false
  properties:
    attribute: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## attributeLevelup

`POST /characters/{id}/ops/attribute.levelup`

Requires that attribute's XP track full (CANNOT_LEVEL_UP), action exists (NOT_FOUND), action below max rating (RATING_MAXED). Increments the action rating and clears that attribute's XP track.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [attribute, action]
  additionalProperties: false
  properties:
    attribute: { type: string, minLength: 1 }
    action: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## actionSetRating

`POST /characters/{id}/ops/action.set-rating`

Direct set, clamped 0..maxRating (game settings ActionPointMaximum).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [action, rating]
  additionalProperties: false
  properties:
    action: { type: string, minLength: 1 }
    rating: { type: integer, minimum: 0 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## abilityTake

`POST /characters/{id}/ops/ability.take`

Validated against playbook settings (TimesTakeable → ABILITY_MAXED). Does NOT spend playbook XP — asymmetric flow, see playbook-xp.clear.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## abilityRemove

`POST /characters/{id}/ops/ability.remove`

Removes the whole ability entry including all stacked takes.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearAdd

`POST /characters/{id}/ops/gear.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [name, bulk]
  additionalProperties: false
  properties:
    name: { type: string, minLength: 1 }
    bulk: { type: integer, minimum: 0 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearRemove

`POST /characters/{id}/ops/gear.remove`

Also removes the item from loadout (sideEffect when it was committed).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearCommit

`POST /characters/{id}/ops/gear.commit`

Preconditions: commitment ≠ none (NO_COMMITMENT), item available (NOT_FOUND), bulk fits (OVER_BULK), not already committed (DUPLICATE), not locked (COMMITMENT_LOCKED).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearUncommit

`POST /characters/{id}/ops/gear.uncommit`

Locked → COMMITMENT_LOCKED.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearSetCommitment

`POST /characters/{id}/ops/gear.set-commitment`

Locked → COMMITMENT_LOCKED. MaxBulk per option comes from game settings.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [commitment]
  additionalProperties: false
  properties:
    commitment: { $ref: "./schemas/common.json#/$defs/commitment" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearClearCommitments

`POST /characters/{id}/ops/gear.clear-commitments`

Clears loadout AND resets commitment to none. Locked → COMMITMENT_LOCKED.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearLock

`POST /characters/{id}/ops/gear.lock`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## gearUnlock

`POST /characters/{id}/ops/gear.unlock`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## fundGain

`POST /characters/{id}/ops/fund.gain`

Satchel fills first, overflow to stash; coins that fit nowhere are reported in sideEffects and applied.effective — never silently dropped.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [coins]
  additionalProperties: false
  properties:
    coins: { type: integer, minimum: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## fundSpend

`POST /characters/{id}/ops/fund.spend`

Satchel first, then stash liquidation at 2 stash → 1 coin. Insufficient → INSUFFICIENT_FUNDS with maxAffordable detail.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [coins]
  additionalProperties: false
  properties:
    coins: { type: integer, minimum: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## fundLiquidate

`POST /characters/{id}/ops/fund.liquidate`

Stash → satchel at 2 stash per 1 coin. Satchel can't fit → SATCHEL_FULL; stash short → INSUFFICIENT_FUNDS.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [coins]
  additionalProperties: false
  properties:
    coins: { type: integer, minimum: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## rolodexAdd

`POST /characters/{id}/ops/rolodex.add`

Duplicate entry → DUPLICATE. New friends start at closeness "friend".

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [entry]
  additionalProperties: false
  properties:
    entry: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## rolodexRemove

`POST /characters/{id}/ops/rolodex.remove`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [entry]
  additionalProperties: false
  properties:
    entry: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## rolodexSetCloseness

`POST /characters/{id}/ops/rolodex.set-closeness`

Full transition set (friend/close-friend/rival) — supersedes the reference's four named upgrade/downgrade methods with one total operation.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [entry, closeness]
  additionalProperties: false
  properties:
    entry: { type: string, minLength: 1 }
    closeness: { $ref: "./schemas/common.json#/$defs/closeness" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## dossierUpdate

`POST /characters/{id}/ops/dossier.update`

Partial update — only provided fields change. crewId must reference an existing crew or be empty string (VALIDATION).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  additionalProperties: false
  minProperties: 1
  properties:
    name: { type: string }
    alias: { type: string }
    look: { type: string }
    notes: { type: string }
    crewId: { type: string }
    background: { $ref: "./schemas/common.json#/$defs/namedDescription" }
    heritage: { $ref: "./schemas/common.json#/$defs/namedDescription" }
    vice: { $ref: "./schemas/common.json#/$defs/namedDescription" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## sessionSet

`POST /characters/{id}/ops/session.set`

Partial update; each value clamped 0..max (game settings).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  additionalProperties: false
  minProperties: 1
  properties:
    playbookExpressions: { type: integer, minimum: 0 }
    characterExpressions: { type: integer, minimum: 0 }
    struggleExpressions: { type: integer, minimum: 0 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## notebookSet

`POST /characters/{id}/ops/notebook.set`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [text]
  additionalProperties: false
  properties:
    text: { type: string }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## endScore

`POST /characters/{id}/end-score`

Composite helper; explicit flags per sub-action so no procedure is encoded. Single snapshot + history entry.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  additionalProperties: false
  minProperties: 1
  properties:
    clearArmorUsed: { type: boolean }
    resetLoadoutCommitment: { type: boolean }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## endDowntime

`POST /characters/{id}/end-downtime`

Composite helper: clears session expressions; optional caller-supplied vice-relief stress clear amount (GM judgment stays outside).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  additionalProperties: false
  properties:
    clearSessionExpressions: { type: boolean }
    viceReliefStress: { type: integer, minimum: 0 }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## listCrews

`GET /crews`

crew summaries

Parameters: none

Request body schema:

None

Responses:

- `200`: crew summaries

## createCrew

`POST /crews`

Parameters: `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [gameStem, crewType]
  additionalProperties: false
  properties:
    gameStem: { type: string, pattern: "^[A-Za-z0-9-]+$" }
    crewType: { type: string, minLength: 1 }
```

Responses:

- `200`: OperationResult

## getCrew

`GET /crews/{id}`

crew DTO (byte-identical to disk)

Parameters: `id`, `download`

Request body schema:

None

Responses:

- `200`: crew DTO (byte-identical to disk)
- `404`: OperationResult

## deleteCrew

`POST /crews/{id}/delete`

confirm:true required. Unlinks member characters (clears their crewId; reported in sideEffects).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Confirm" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## importCrew

`POST /crews/{id}/import`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Crew" }
```

Responses:

- `200`: OperationResult
- `400`: OperationResult
- `404`: OperationResult
- `409`: OperationResult
- `413`: OperationResult

## listCrewHistory

`GET /crews/{id}/history`

snapshot list, newest first

Parameters: `id`

Request body schema:

None

Responses:

- `200`: snapshot list, newest first
- `404`: OperationResult

## getCrewSnapshot

`GET /crews/{id}/history/{snapshotId}`

snapshot DTO

Parameters: `id`, `snapshotId`

Request body schema:

None

Responses:

- `200`: snapshot DTO
- `404`: OperationResult

## undoCrew

`POST /crews/{id}/undo`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## heatAdd

`POST /crews/{id}/ops/heat.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## wantedAdd

`POST /crews/{id}/ops/wanted.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## repAdd

`POST /crews/{id}/ops/rep.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## tierAdd

`POST /crews/{id}/ops/tier.add`

Bounded below at 0; applied delta reported.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## holdSet

`POST /crews/{id}/ops/hold.set`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [hold]
  additionalProperties: false
  properties:
    hold: { $ref: "./schemas/common.json#/$defs/hold" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## crewXpAdd

`POST /crews/{id}/ops/xp.add`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## crewXpClear

`POST /crews/{id}/ops/xp.clear`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## crewAbilityTake

`POST /crews/{id}/ops/ability.take`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## crewAbilityRemove

`POST /crews/{id}/ops/ability.remove`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## upgradeMark

`POST /crews/{id}/ops/upgrade.mark`

One box; TotalBoxes from crew game settings → UPGRADE_MAXED.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## upgradeUnmark

`POST /crews/{id}/ops/upgrade.unmark`

Box-wise: removes ONE box; entry disappears only at 0 boxes. Deliberate divergence from the C# reference (which removed the whole upgrade).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Named" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## cohortAdd

`POST /crews/{id}/ops/cohort.add`

{ type: string }

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [cohortKind]
  additionalProperties: false
  properties:
    cohortKind: { $ref: "./schemas/common.json#/$defs/cohortType" }
    gangType: { type: string }
    expertType: { type: string }
    quality: { type: integer, minimum: 0 }
    scale: { type: integer, minimum: 0 }
    hasArmor: { type: boolean }
    edges: { type: array, items: { type: string } }
    flaws: { type: array, items: { type: string } }
    description: { type: string }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## cohortUpdate

`POST /crews/{id}/ops/cohort.update`

Partial update by cohortId.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [cohortId]
  additionalProperties: false
  minProperties: 2
  properties:
    cohortId: { type: string }
    gangType: { type: string }
    expertType: { type: string }
    quality: { type: integer, minimum: 0 }
    scale: { type: integer, minimum: 0 }
    hasArmor: { type: boolean }
    edges: { type: array, items: { type: string } }
    flaws: { type: array, items: { type: string } }
    harm: { $ref: "./schemas/common.json#/$defs/cohortHarm" }
    description: { type: string }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## cohortRemove

`POST /crews/{id}/ops/cohort.remove`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  required: [cohortId]
  additionalProperties: false
  properties:
    cohortId: { type: string }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## coinAdd

`POST /crews/{id}/ops/coin.add`

Bounded below at 0; applied delta reported.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## stashAdd

`POST /crews/{id}/ops/stash.add`

Bounded below at 0; applied delta reported.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Delta" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## crewFieldsUpdate

`POST /crews/{id}/ops/fields.update`

Partial update of free-text crew fields.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `true`

Request body schema:

```yaml
schema:
  type: object
  additionalProperties: false
  minProperties: 1
  properties:
    name: { type: string }
    lair: { type: string }
    reputation: { type: string }
    huntingGrounds: { type: string }
    notes: { type: string }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## listClocks

`GET /clocks`

all campaign clocks

Parameters: none

Request body schema:

None

Responses:

- `200`: all campaign clocks

## createClock

`POST /clocks`

Parameters: `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [name, clockKind, size]
  additionalProperties: false
  properties:
    name: { type: string, minLength: 1 }
    clockKind: { enum: [project, rollover] }
    size: { type: integer, minimum: 1 }
```

Responses:

- `200`: OperationResult

## getClock

`GET /clocks/{id}`

clock DTO

Parameters: `id`

Request body schema:

None

Responses:

- `200`: clock DTO
- `404`: OperationResult

## deleteClock

`POST /clocks/{id}/delete`

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema: { $ref: "#/components/schemas/Confirm" }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## clockProgress

`POST /clocks/{id}/ops/clock.progress`

Project clocks clamp at full; rollover clocks carry overflow (applied on reset).

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

```yaml
schema:
  type: object
  required: [segments]
  additionalProperties: false
  properties:
    segments: { type: integer }
```

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult

## clockReset

`POST /clocks/{id}/ops/clock.reset`

Rollover clocks re-apply carried overflow after reset.

Parameters: `id`, `ifMatch`, `idempotencyKey`

Snapshot: `false`

Request body schema:

None

Responses:

- `200`: OperationResult
- `404`: OperationResult
- `409`: OperationResult
